{% extends 'base.html.twig' %}


{% block title 'Agence bidule' %}


{% block body %}

    <div class="jumbotron text-center">
        <h1>Agence bidule</h1>

        <p> Lorem ipsum dolor sit amet, consectetur adipisicing elit. Neque fugit sed aliquid odio quam, ab quibusdam delectus totam itaque veritatis velit perferendis saepe. Natus deserunt cum, iste debitis id dolores? </p>

    </div>
    <div class="container">
        <h2>Les derniers biens</h2>
        {% for property in properties %}
            <div class="col-3">
                <div class="card">
                    <div class="car-body">
                        <h5 class="card-title">
                        <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">
                        {{ property.title }}
                        </a>
                        </h5>
                        <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>
                        <div class="text-primary" style=" font-weight: bold; font-size: 2rem ">{{ property.formatedPrice}} €</div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}